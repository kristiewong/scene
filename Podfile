# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Scene' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Scene
  pod 'Firebase/Core'
  pod 'FirebaseAuth'
  pod 'FirebaseStorage'
  pod 'SDWebImage'
  pod 'MBProgressHUD'
  pod 'SDWebImage'
  pod 'FirebaseDatabase'


  target 'SceneTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SceneUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
